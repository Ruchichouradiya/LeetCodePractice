class Solution {
    static int countSquares(int N) {
        
        int count =0;
        for(int i=1;i<Math.sqrt(N);i++){
            if(Math.sqrt(1)<N){
                count++;
            }else
                break;
        }
        return count;
    }
}
